mss>=10.2.0
Pillow>=10.0.0
paho-mqtt>=2.1.0
# Optional: pywin32 (Windows only, for window title detection)
# pywin32>=306

requests>=2.33.1

# Optional: only needed for capture_agent_bridge.py (HDMI bridge on Pi/SBC)
# opencv-python>=4.8.0
