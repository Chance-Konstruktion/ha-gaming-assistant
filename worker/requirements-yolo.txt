# Requirements for the YOLO Object Detection Worker
# Install on a machine with a GPU for best performance,
# or use a Raspberry Pi with NCNN/Hailo backend.
ultralytics>=8.4.41
paho-mqtt>=2.1.0
Pillow>=10.0.0

# --- Backend-specific (install ONE matching your hardware) ---

# Desktop GPU (NVIDIA CUDA):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Raspberry Pi (NCNN — recommended for RPi 3/4/5 CPU):
# pip install ncnn

# Raspberry Pi 5 + AI Kit (Hailo-8L NPU, 13 TOPS):
# pip install hailo-platform
# Note: requires Hailo SDK and pre-compiled .hef model files
# See: https://github.com/hailo-ai/hailo-rpi5-examples

# Raspberry Pi (TFLite — lightweight alternative):
# pip install tflite-runtime
